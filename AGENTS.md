# Things for agent to ignore

- In this repo the current proxy error that occurs for the spotify api is not something that needs fixing,
this is because the api server is not running in dev only in production 
